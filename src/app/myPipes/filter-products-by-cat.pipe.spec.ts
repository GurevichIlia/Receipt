import { FilterProductsByCatPipe } from './filter-products-by-cat.pipe';

describe('FilterProductsByCatPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProductsByCatPipe();
    expect(pipe).toBeTruthy();
  });
});
